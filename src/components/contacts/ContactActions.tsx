
import { Button } from '@/components/ui/button';

interface ContactActionsProps {
  selectedContact?: any;
}

export const ContactActions = ({
  selectedContact
}: ContactActionsProps) => {
  return null;
};
